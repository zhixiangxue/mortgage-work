# --- Python ---
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Node / frontend ---
frontend/node_modules/
frontend/dist/

# --- Secrets: commit .env.example, never the real .env ---
.env
server/.env

# --- Auth service state: user DB, HMAC secret, locally-provisioned repos ---
server/data/

# --- Runtime artefacts ---
runtime.log
runtime.log.1

# --- OS / editor cruft ---
.DS_Store
tmp/
dist/
node_modules/

# --- PyInstaller ---
build/
*.spec.bak

# --- Vendored third-party binaries (fetched at build time, shipped in dist) ---
vendor/

# --- Independent work-repos: app depends on them but they are cloned separately ---
mortgage-skills/
nmls-10293847/
.qoder/
playground/